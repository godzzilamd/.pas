Program P1;

var x,n,i,j:byte;
    s:integer;
    a : array [0 .. 100] of byte;

begin
write('n = '); readln(n);
write('x = '); readln(x);
for i := 0 to n do
begin
    while (s <> n) do
    begin
        
    end;    
end;
readln;
end.
